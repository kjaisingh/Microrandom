// initialGroups is a 3D array
// the first index indicates the number of initial seed groupings
// the second index indicates the number of groups in the grouping
// the third index indicates the number of members in a particular group

function geneticAlgorithm(initialGroups) {
  return initialGroups;
}

module.exports = geneticAlgorithm;
